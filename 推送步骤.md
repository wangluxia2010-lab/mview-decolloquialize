# 推送步骤（手动版）

如果您不想用 `push.ps1`，按下面五步手动执行即可。全程只有您自己接触凭据。

## 第 1 步：在 GitHub 建一个**私有**仓库

1. 打开 https://github.com/new
2. Repository name 填一个名字，例如 `mview-decolloquialize`
3. **Visibility 一定选 Private**（手册含客户项目细节与记者真实姓名）
4. **不要**勾选 "Add a README file"（保持空仓库，避免推送冲突）
5. Create repository

## 第 2 步：生成一个短期、最小权限的令牌

1. GitHub 右上角头像 → Settings → Developer settings → **Fine-grained tokens** → Generate new token
2. Repository access：只选刚才那一个仓库
3. Permissions → Repository permissions → **Contents: Read and write**（其他都不给）
4. 有效期设为 7 天
5. 生成后复制令牌（只显示一次）

## 第 3 步：在本目录打开终端

Windows：在本文件夹地址栏输入 `powershell` 回车；或右键「在终端中打开」。

## 第 4 步：执行推送

```powershell
# 若 git 不在 PATH，用便携版路径（本机已验证存在）
$git = "$env:USERPROFILE\.qwenworkcn\bin\git\mingw64\bin\git.exe"

cd <本目录路径>          # 例如 cd "$env:USERPROFILE\Downloads\mview-decolloquialize"

& $git config --global user.name  "Lucia Wang"
& $git config --global user.email "你的GitHub用户名@users.noreply.github.com"

& $git init -b main
& $git add .
& $git commit -m "Add 去口语化工作手册与 steno-decolloquialize 技能包 v2.2"

# 令牌只出现在这一条命令里，不写入 .git/config
& $git push https://x-access-token:<粘贴你的令牌>@github.com/<你的用户名>/mview-decolloquialize.git main:main
```

看到 `main -> main` 即成功。

## 第 5 步：收尾（重要）

1. **撤销令牌**：GitHub → Settings → Developer settings → Fine-grained tokens → 该令牌 → Delete/Revoke
2. **再确认仓库是 Private**：仓库页 Settings → Details → Visibility
3. 如果终端里执行过带令牌的 push 命令，建议清一下命令历史：
   ```powershell
   Clear-History
   ```
   （PowerShell 的历史默认不落盘；若您用的是 cmd 或把命令存进了脚本，请删除该记录）

## 以后更新怎么推

仓库里已经配好了 `origin`（不含令牌）。之后每次更新技能或手册：

```powershell
& $git pull origin main
# 覆盖 skill/SKILL.md 与 skill/steno-decolloquialize.skill，并更新 handbook 下的文件
& $git add .
& $git commit -m "Update 技能到 v2.3：新增XXX"
& $git push https://x-access-token:<新令牌>@github.com/<你的用户名>/mview-decolloquialize.git main:main
```

## 团队怎么用

1. 同事在仓库 `skill/` 目录下载 `steno-decolloquialize.skill`
2. 拖进千问办公对话框 → 点卡片上的「保存技能」
3. 以后说一句「去口语化」或「把这份速记改书面」，就会按手册标准执行
4. 培训时用 `handbook/去口语化工作手册_v1.6.html`（手机/电脑浏览器直接打开）或打印 `v1.5.docx`

## 版本纪律提醒

本仓库自带版本记录表。**任何修改都要升版本号**——文件名、封面标注、版本表三处同步。改内容不升版本会断掉追溯链，这是团队硬性要求。
